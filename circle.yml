machine:
  node:
    version: 0.10.22
  services:
    - docker
    - mongo

dependencies:
  pre:
    - pip install awscli


deployment:
  staging:
    branch: master
    heroku:
      appname: taaas

